// Find out the difference between two integers

#include <stdio.h>

void main()
{
    int a = 20;
    int b = 10;
    int diff = a - b;
    printf("Difference of %d and %d is %d", a, b, diff);
}